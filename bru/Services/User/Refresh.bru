meta {
  name: Refresh
  type: http
  seq: 5
}

post {
  url: {{host}}/auth/refresh
  body: json
  auth: inherit
}

body:json {
  {
    "refreshToken": "{{refreshToken}}"
  }
}

tests {
  const response = res.getBody()
  
  bru.setVar("accessToken", response.accessToken)
}

settings {
  encodeUrl: true
  timeout: 0
}
