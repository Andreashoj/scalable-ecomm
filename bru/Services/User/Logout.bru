meta {
  name: Logout
  type: http
  seq: 4
}

post {
  url: {{host}}/auth/logout
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{accessToken}}
}

body:json {
  {
    "refreshToken": "{{refreshToken}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
