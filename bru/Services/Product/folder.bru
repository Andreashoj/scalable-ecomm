meta {
  name: Product
}

headers {
  Authorization: Bearer {{accessToken}}
}

auth {
  mode: bearer
}

auth:bearer {
  token: 
}

vars:pre-request {
  host: http://localhost:8080/api/product
  ~host: http://localhost:8080
}
